{% extends 'base.html.twig' %}

{% block title %}Modification de l'endroit{% endblock %}

{% block body %}
    <h1>Modifier l'endroit</h1>

    {{ include('country_form/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('country_index') }}">revenir à l'accueil</a>
    {% if is_granted('ROLE_ADMIN') %}
        {{ include('country_form/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
